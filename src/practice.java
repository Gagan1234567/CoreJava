
import java.io.*;
import java.util.*;
import java.util.stream.Collectors;

class practice {


    // Driver code
    public static void main(String args[]) {


//        String s = "mississippi";
//        Character str= Arrays.stream(s.split("")).
//                collect(Collectors.groupingBy(Fun,collectors.counting())).entrySet.stream
//                .filter(x->x.getKey()==1).map(x->x.getkey()).findFirst().get();
//        System.out.println(str);

//        List<Employee> employees = Arrays.asList(
//                new Employee(50000, "John", 1, 25, "Male"),
//                new Employee(60000, "Jane", 2, 22, "Female"),
//                new Employee(70000, "Bob", 3, 30, "Male"),
//                new Employee(80000, "Alice", 4, 18, "Female"),
//                new Employee(90000, "Charlie", 5, 28, "Male")
//        );
//
//        Map<String, Long> genderCountMap = employees.stream()
//                .filter(employee -> employee.getAge() > 20)
//                .collect(Collectors.groupingBy(
//                        Employee::getGender,
//                        Collectors.counting()
//                ));
//
//        System.out.println("Gender Count Map: " + genderCountMap);


//        // Reverse the order of the words using Java 8 streams
//        String reversedString = Arrays.stream(words)
//                .collect(Collectors.toList())
//                .stream()
//                .reduce((first, second) -> second + " " + first)
//                .orElse("");

//        String reversedString = Arrays.stream(words)
//                .collect(Collectors.toList())
//                .stream().skip(1).findFirst().orElse("no String");

//        List<Employee> listOfMaleAndFemale=li.Stream().Filter(n->n.getAge>20).
//StringBuilder sb=new StringBuilder();
//for(int i=0;i<=wordList.size()-1;i++){
//    sb.append(" ").append(wordList.get(i));
//}
//        System.out.println(sb);

    }


}